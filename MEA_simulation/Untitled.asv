beta_val = [0.000975];
%eta_val = [0.02, 0.021, 0.022, 0.023, 0.04, 0.045, 0.05];
eta_val = linspace(0.02,0.05,20);
IP3_val = [0.4778];

freq = []

for k = 1:length(IP3_val)
    for j = 1:length(eta_val)
        for i = 1:length(beta_val)
            [~,~,~,~,peaks] = imtiaz_2002d_noTstart_COR_exported(beta_val(i), eta_val(j), IP3_val(k), 1);
        end
    end
end

plot(eta_val, peaks)